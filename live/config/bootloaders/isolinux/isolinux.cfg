serial 0 115200
include menu.cfg
default vesamenu.c32
prompt 0
# Must come after the include: syslinux parses sequentially, and the menu
# module reads TIMEOUT 0 as "wait forever", which would strand headless
# machines at the menu. menu.cfg adds ontimeout live-amd64.
timeout 100
